import React from "react";
import { PageContent, Heading } from "bumbag";

const RPPage: React.FC = () => {
  return (
    <PageContent>
      <Heading>Reward Page Name</Heading>
    </PageContent>
  );
};

export default RPPage;
